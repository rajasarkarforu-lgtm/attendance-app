name: attendance_app
description: Tuition attendance app

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0

flutter:
  uses-material-design: true
